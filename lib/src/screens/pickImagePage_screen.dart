import 'package:flutter/material.dart';

class PickImagePageScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Uso de Pick image"),
      ),
      body: Center(),
    );
  }
}
